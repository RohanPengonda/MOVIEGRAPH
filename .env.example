# MovieGraph Environment Configuration
# CognoDB / Neo4j Database Configuration (Bolt Protocol)
COGNODB_URI=bolt://localhost:7687
COGNODB_USERNAME=cognodb
COGNODB_PASSWORD=password

# Server Port
PORT=5000

# TMDB API Key (Optional: seed script uses fallback dataset if omitted)
TMDB_API_KEY=
